const TAB = "      ";
const SPACE = " ";
const NEWLINE = "\n";
const EMPTYSTRING = "";

const add = (x, y) => x + y;
const splitByNewLine = x => x.split(NEWLINE);

module.exports = {
  TAB,
  SPACE,
  NEWLINE,
  EMPTYSTRING,
  add,
  splitByNewLine
};
