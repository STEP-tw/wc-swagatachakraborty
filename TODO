* lib.js need to rename
* lib.js output should be in array of object 
    like -  
    [
        {
          file :
          exist :
          lines :
          words :
          byte :
        },
        {
            //
        }
    ]

* write formater that will format the output -> separate file

*lib.js -
    * write a object that contain all kind of counter

*util -
    * add separate file for string util and numeric utils

*libTest -
    * mock fs should be in global scope
